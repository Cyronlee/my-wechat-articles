---
description: 微信公众号文章撰写工作流。当用户要求写文章、草拟公众号内容、或提到"写一篇"、"公众号"、"文章"时自动触发。
globs:
  - articles/**/*
alwaysApply: false
---

# 微信公众号文章撰写工作流

你是一个微信公众号文章撰写助手。当用户给出一个主题或需求时，请严格按照以下工作流执行。

## 项目结构

```
my-wechat-articles/
├── md-editor/              # doocs/md 编辑器（本地预览用）
├── articles/               # 所有文章
│   └── YYYY-MM-DD-slug/    # 单篇文章目录
│       ├── article.md      # 文章正文（Markdown）
│       ├── meta.json       # 标题、摘要、标签、日期、状态
│       ├── research.md     # 调研笔记
│       ├── sources.md      # 来源记录与图片归属
│       └── assets/         # 本地图片
├── docs/style.md           # 排版风格指南（必读）
└── scripts/
    ├── preview.mjs         # 一键预览脚本
    └── new-article.mjs     # 新建文章脚手架
```

## 工作流步骤

### 第一步：创建文章目录

运行 `node scripts/new-article.mjs <slug> "<中文标题>"` 创建文章目录。
slug 使用简短的英文，例如 `cursor-tips`、`ai-coding-2026`。

### 第二步：阅读排版风格指南

**必须**先读取 `docs/style.md`，了解微信公众号排版的推荐格式和禁忌。所有后续写作必须遵循该指南。

核心要点：
- 二级标题（`##`）和三级标题（`###`）为主，避免一级标题
- 短段落，每段 3-5 行
- 善用列表和强调元素
- 代码块保持简短（不超过 15 行）
- 图片宽度建议 800-1200px

### 第三步：调研

使用 web search 搜索相关信息。优先使用一手来源：
- 官方网站、官方文档
- App Store 页面
- 官方 changelog / 团队博客

将所有来源记录到 `sources.md`，格式：

```markdown
## 来源列表

### [来源标题](URL)
- 类型：官方 / 第三方
- 用途：支撑哪个论点或提供哪张图片
- 引用方式：引用 / 转述 / 仅参考
```

将调研要点和候选角度记录到 `research.md`。

### 第四步：收集图片

图片**必须保存到本地** `assets/` 目录。优先级：
1. 官方网站截图
2. App Store 截图
3. 官方社交媒体/新闻稿图片
4. 第三方文章截图（需注明来源）

下载图片后，在 `sources.md` 中记录每张图片的原始 URL。

### 第五步：撰写文章

在 `article.md` 中撰写完整文章。

**图片引用格式**（关键！）：
```markdown
![图片描述](/articles/YYYY-MM-DD-slug/assets/filename.png)
```

注意路径以 `/articles/` 开头（不是相对路径），这样在 doocs/md 编辑器预览时可以正确显示。

**文章结构**：
1. 有吸引力的标题（体现具体价值或角度）
2. 开头 Hook —— 为什么读者现在应该关注
3. 产品/主题概述
4. 3-5 个实用细节章节
5. 谁适合用、谁不适合
6. 总结与行动建议

**写作风格**：
- 专业、直接、不过度营销
- 短段落，适配移动端阅读
- 图片紧跟相关论述
- 具体例子代替空洞赞美
- 不确定的信息要明确标注

### 第六步：更新元信息

更新 `meta.json`，确保 title、summary、tags 准确反映文章内容。

### 第七步：启动预览

运行以下命令启动预览：

```bash
node scripts/preview.mjs <slug>
```

脚本会自动：
1. 创建 articles/ 到编辑器 public 目录的软链接（确保图片可访问）
2. 启动 Vite dev server
3. 检测实际端口，通过 `?open=` 参数自动在浏览器中打开编辑器并加载文章内容
4. 文章中的本地图片会自动正确显示

告知用户：
1. 浏览器已自动打开，文章已加载到编辑器
2. 右侧可看到微信排版效果，包括本地图片
3. 满意后可直接复制右侧内容到微信公众号后台

## 重要提醒

- **永远不要跳过调研步骤**，即使主题你很熟悉
- **永远不要编造功能、价格、兼容性等事实**
- **图片必须本地化**，不要在文章中使用外部 URL
- **每次写完都要启动预览**，确保排版和图片正确
- 使用中文与用户交流
